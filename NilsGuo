x = int(raw_input('Your money:'))
list = [1, 3, 2, 4, 6, 5, 1, 2]
list.sort()
items = []
def function(x,list):
    sum = 0
    for i in list:
        sum = sum + i
        if sum > x:
            break
        else:
            items.append(i)
    return len(items)
print function(x,list)
